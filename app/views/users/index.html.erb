<div class="container">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <%= render 'info', user: current_user %>
      <h2 class="mt-3">New book</h2>
      <%= render 'books/form', book: @book %>
    </div>

    <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <%= link_to "グループを作成する", new_user_group_path(current_user.id) %>|<%= link_to "グループ一覧", user_groups_path(current_user) %>
      <%= render 'index',users: @users %>
    </div>
  </div>
</div>








